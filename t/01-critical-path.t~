#!perl -T
use 5.006;
use strict;
use warnings;
use Test::More;

plan tests => 1;

BEGIN {
    use_ok( 'SubSystem::CachedDB' ) || print "Bail out!\n";
}

diag( "Testing SubSystem::CachedDB $SubSystem::CachedDB::VERSION, Perl $], $^X" );
